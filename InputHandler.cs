using System;
using System.Collections.Generic;
using System.Text;

namespace JenPile {
    class InputHandler {

        // Remove commented lines

        //divide up by spaces

        //move all seperators and operators to their own lines 

        // list of broken up input gets sent to FSM





        string separators = @"(;)";


       // public void BreakLineIntoParts(string line) {
       //     if (line[0] != '!') {
                // string[] tempLine = line.Split(new char[] { ' ', '\t' }, StringSplitOptions.RemoveEmptyEntries);
               // string[] tempLine = Regex.Split(line, separators);
      //          foreach (string s in tempLine) {
                   //lineList.Add(s);
      //          }
       //     }
      //  }

    }
}
