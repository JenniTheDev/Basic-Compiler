namespace JenPile {
    public enum ParseType {
        NONE,
        ENDOFFILE,
        EXPRESSION,
        IDENTIFIER,
        STATEMENT,
        ASSIGN,
        TERM,
        FACTOR,
        INTEGER,
        UNDEFINED
    }
}
